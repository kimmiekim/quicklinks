/* @flow weak */

import React from 'react';

const QuicklinkBox = (props) =>
  {
    // props.isChecked === true ? box : '';
    // console.log(props)
    <div>
      <p>erin!</p>
      {/* <p>{props.name}</p> */}
    </div>
  }


export default QuicklinkBox;
